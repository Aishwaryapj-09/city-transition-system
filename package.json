{
  "name": "city-transition-system",
  "private": true,
  "scripts": {
    "reports:prepare": "node -e \"require('fs').mkdirSync('devsecops-reports',{recursive:true})\"",
    "preapi:test": "npm run reports:prepare --silent",
    "preapi:test:smoke": "npm run reports:prepare --silent",
    "preapi:test:deployed": "npm run reports:prepare --silent",
    "api:test": "newman run postman/city-transition-api.postman_collection.json -e postman/local.postman_environment.json --reporters json,junit --reporter-json-export devsecops-reports/postman-api-full-report.json --reporter-junit-export devsecops-reports/postman-api-full-report.xml",
    "api:test:smoke": "newman run postman/city-transition-api.postman_collection.json -e postman/local.postman_environment.json --folder \"CD Smoke\" --reporters json,junit --reporter-json-export devsecops-reports/postman-api-smoke-local-report.json --reporter-junit-export devsecops-reports/postman-api-smoke-local-report.xml",
    "api:test:deployed": "newman run postman/city-transition-api.postman_collection.json -e postman/local.postman_environment.json --folder \"CD Smoke\" --env-var baseUrl=http://localhost:30008 --reporters json,junit --reporter-json-export devsecops-reports/postman-api-smoke-report.json --reporter-junit-export devsecops-reports/postman-api-smoke-report.xml",
    "reports:dashboard": "node tools/devsecops-dashboard.js",
    "reports:endpoints": "node tools/devsecops-dashboard.js --collect-only",
    "reports:zip": "powershell -NoProfile -ExecutionPolicy Bypass -File tools/package-devsecops-reports.ps1"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "sonar-scanner": "^3.1.0"
  }
}
