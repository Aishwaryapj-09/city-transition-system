apiVersion: v1
kind: Service
metadata:
  name: accommodation-service   # ✅ REQUIRED
spec:
  selector:
    app: accommodation-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort